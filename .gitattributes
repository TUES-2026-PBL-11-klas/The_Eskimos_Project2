*.mbtiles filter=lfs diff=lfs merge=lfs -text

# Force LF line endings on shell scripts so they run inside Linux containers
# even when checked out on Windows (otherwise CRLF breaks the shebang -> exit 127).
*.sh text eol=lf
